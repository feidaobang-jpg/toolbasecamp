---
description: 开工前自动检查并 ff-only 拉取最新代码（有本地改动则不拉）
alwaysApply: true
---

# Git 同步：开工前 pull（安全版）

本仓库多会话并行改代码并 push。Agent **开始实质性任务前**（改代码、SSH/部署、按规则办事）先对齐远端，避免用过时规则/脚本。

## 何时执行

在下列时机 **各做一次**（同一会话内已成功同步过可跳过，除非间隔很长或切换了分支）：

1. 会话开始后、动手改文件 / 跑部署 **之前**
2. SSH 运维、改 VPS 配置 **之前**
3. 用户明确说「拉最新 / sync / pull」时

闲聊、纯问答且不依赖仓库现状时可不拉。

## 标准步骤

在仓库根目录：

```bash
git fetch origin
git status -sb
```

然后：

| 状态 | 动作 |
|------|------|
| 工作区干净，且仅落后于 `origin/<当前分支>`（可快进） | `git pull --ff-only` |
| 有未提交改动（modified / untracked 要保留） | **不 pull**；继续基于当前工作区，必要时提醒「本地有改动未拉远端」 |
| 与远端分叉、无法 ff-only | **不 pull / 不 merge / 不 rebase**；向用户说明并询问 |
| 已与远端一致 | 无需再 pull |

## 禁止

- `git pull` 不带保护（本仓库默认只用 `--ff-only`）
- 为了 pull 自动 `git stash` / `reset --hard` / `clean -fd`（除非用户明确要求）
- 在正在半截编辑、未提交的本地改动上强行覆盖
- 用 pull 代替查看用户当前分支意图（不要擅自切分支）

## 与 auto-commit-push 的关系

- **拉**：开工前、工作区干净时 ff-only
- **推**：改完 tracked 文件后按 `auto-commit-push` 提交并 push
- 二者不互相替代；有本地未提交改动时优先完成/提交当前工作，再考虑 pull
