# 图生图 / 指令改图：保持输入宽高比

指令改图、图生图（含多模型对比）在各厂商 API 允许范围内，**必须尽量保持输入参考图的宽高比**；禁止无参考信息时默认强制 1:1，导致竖屏截图被压成方图或卡片里「半截收据」。

## 目标（现实边界）

- **优先**：输出宽高比贴近首张参考图（容差内选最近官方档位）。
- **分辨率**：按用户选的 1K/2K 档位（或厂商等价档）缩放面积，**不要求**与原图像素宽高完全一致（多数 API 做不到精确同分辨率）。
- **禁止**：图生图路径把 `size`/`aspectRatio` 写死成正方形，而忽略已有参考图尺寸。

## 实现

共享计算：`server/image_i2i_size.py`

| 厂商 / 模型 | 做法 |
|---|---|
| 万相 Wan / 千问 Image | DashScope `parameters.size` 用 `W*H`（`pixel_size_star`），不要只用 `1K`/`2K` 忽略比例 |
| 豆包 Seedream | Ark `size` 用最近 `WIDTHxHEIGHT` 预设（`seedream_size_wh`），不要只用 `2k` 方图档 |
| GPT Image 2（lk888） | `/images/edits` 的 `size` 用最近 `1024x1024` / `1024x1536` / `1536x1024`（`gpt_size_wh`） |
| Banana（lk888 media） | `aspectRatio` + `imageSize` 由参考图选最近档（`banana_aspect_and_tier`） |

参考尺寸优先用**上传原图**（normalize/JPEG 压缩前）的宽高。

## 前端展示

- 结果卡 `.instruct-result-card img`：**不要**用过小的 `max-height` 裁掉竖图底部（按钮也看不见）；满宽 + `height: auto` + `object-fit: contain`。
- 缩略图网格另论；结果卡要看清整图与操作按钮。

## 禁止

- 新增 I2I 引擎却写死 `1:1` / `1024x1024` / `2k` 且不读参考图。
- 以「UI 好看」为由把竖图结果卡压成固定矮框导致内容与按钮被裁切。
- 声称「保持原分辨率」却实际只改 CSS、API 仍出方图。

## 自检

用竖屏支付截图等多模型对比：各卡结果应接近竖长比，底部「再改 / 下载 / 公开」可见；GPT 等仅支持离散档时允许略偏（如 2:3），但不得整卡变方图。
