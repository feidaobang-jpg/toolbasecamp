---
description: 输入图上传区紧凑、选图后半宽正方形预览（全局 UI）
alwaysApply: true
---

# 输入图：空区紧凑 + 选图后半宽正方形

凡用户**选择 / 拖入本地图片**并展示预览（工具页、家里电脑、内容页等）：

1. **空上传 / 拖放区**：宽度可拉满，但**不要**一上来就做成满屏大正方形；用紧凑条（约 `min-height: 112px` + padding），选图后再出预览。
2. **选图后预览**：宽度约 **容器一半**（`max-width: min(50%, 420px)`；手机可满宽），**高度 = 宽度**（`aspect-ratio: 1 / 1`）。未选图时预览容器必须 `hidden`，且 CSS 里对带 `[hidden]` 的预览写 `display: none !important`（否则 `display:flex` 会盖掉原生 hidden，出现空灰框）。
3. 图用 `object-fit: contain`，背景可用浅灰。
4. **禁止**再压成 168/220/280px 小缩略图；也**不要**空态就占满一屏高。

## 必用 class

| 场景 | class |
|---|---|
| 空上传 / 拖放区 | `drop-zone tb-input-image` |
| 选图后预览容器 | `tb-input-image-preview`（可叠加 `home-pc-source-grid` / `instruct-source-grid` / `img-tool-source` / `i23d-ref-preview` 等） |

样式唯一定义在 **`public/css/docs/docs-common.css`**（输入图全局段）。页面须引入 `docs-common.css`。

家里电脑若有覆盖，在 `home-pc.css` 里只能**对齐**上述尺寸，禁止空态再改回满屏正方形，也禁止预览再压成迷你图。

## CSS 写法（避免失效）

把 `aspect-ratio: 1 / 1` + `height: auto` 写在 **选图后的 `<img>`** 上，配合预览容器半宽。  
空 `.tb-input-image`：**不要**设 `aspect-ratio: 1 / 1`。  
卡片上若另有「清空」等按钮，按钮放在正方形 `img` **下方**。

## 多图

多张输入图：**纵向叠放**，每张各自半宽正方形（同一预览栏宽）；不要缩成多列小网格（对比用的结果网格除外）。

## 例外（不要套本规则）

- **结果图 / 历史缩略图网格**（输出列表、流水线候选卡）——仍可小缩略图。
- **3D mesh 预览**（`.i23d-viewer`）：与输入预览同档（约半宽正方形）；未生成前不要空占满屏。
- **非图片**上传（PDF / 视频 / 音频）。
- 纯本地编辑画布若用户明确要求按原图像素比例展示输出，输出区可例外；**输入选择区仍遵守本规则**。

## 禁止

- 新建 `accept="image/*"` 上传却不挂 `tb-input-image` / 预览不挂 `tb-input-image-preview`。
- 空上传区做成满宽大正方形占一整屏。
- 以「桌面太空」为由把选图后预览改回扁条或超小方块。

## 改完

递增引用页的 `docs-common.css?v=`（及若改了 `home-pc.css` / `image-tools.css` 的 `?v=`）。
