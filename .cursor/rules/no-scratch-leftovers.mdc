---
description: 禁止把一次性探测/调试脚本留在仓库里
alwaysApply: true
---

# 临时脚本：用完即删

排查、探测、dump 配置等**一次性**脚本（如 `_find_*.py`、`_dump_*.py`、`_tmp_*`）：

1. **优先**用 shell 一次性命令或系统临时目录，不要写进仓库目录。
2. 若必须落盘：放系统临时目录，或仓库外路径；**任务结束前删除**，不要留下 untracked。
3. **禁止**把这类文件 `git add` / commit；**不要**为它们改 `.gitignore` 开大口子（例如忽略整个 `deploy/_*.py`），以免误藏真脚本。
4. 需要长期保留的运维脚本：用正式名（无 `_dump` / `_tmp` 前缀），放 `deploy/`，并说明用途。
