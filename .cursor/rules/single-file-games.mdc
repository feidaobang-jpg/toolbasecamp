---
description: 休闲小游戏约定：单 HTML、程序化绘制；网页/手机双端适配；按需可引入引擎
globs: public/html/game/**/*
alwaysApply: true
---

# 单文件休闲游戏约定

新增或大改「可双击运行」的休闲小游戏时，默认遵守下列约束（与《潜水捕鱼》《跑酷》同标准）。

## 形态

- **优先单 HTML 文件**：`public/html/game/<name>.html`，内联 CSS + JS（可用 `type="module"`）。
- **默认零外部依赖**：不引用图片/音频/字体素材包；不拆成必须额外下载贴图才能玩的资源。
- **2D Canvas 优先**：零依赖下用 Canvas 2D 程序化绘制。
- **可引入引擎（按需）**：用户明确要求 3D / 复杂场景时，允许通过 CDN 引入 **Three.js、Babylon.js** 等引擎（`importmap` 或 ESM CDN）。引擎只负责渲染与数学；场景与角色仍优先用代码几何体/材质色块，不默认上贴图。
- **裸 WebGL**：仅在明确要求「无引擎」时使用。
- 可接入游戏中心（`config.js` 的 `gamesConfig` + 中英文 `locales`），但游戏本体仍须能单独打开运行（允许依赖一次 CDN；离线不可玩需在标题说明）。

## 必须具备

1. **程序化外观**  
   场景、角色、UI 装饰优先代码生成；不依赖贴图包。使用引擎时用 `BoxGeometry` / `Capsule` / 纯色 `MeshStandardMaterial` 等即可。

2. **音效（按需）**  
   默认可用 `AudioContext` 合成短音效；**若用户明确不要声音，则不要加**。不引用外部音频文件。移动端需在用户手势后 `resume`。

3. **localStorage 存档**  
   至少持久化最高分；有成长/天数/中途进度的游戏应支持「继续」。键名带项目前缀（如 `tbc_<game>_v1`）。

4. **触屏操作**  
   手机可玩：虚拟按钮 / 滑杆 / 分区点击等；与键鼠并存。`touch-action: none`，`preventDefault` 避免滚动抢手势。

5. **网页 + 手机双端布局（必须）**  
   - 用 `resize` / `aspect ratio` 适配，勿写死仅适合横屏桌面的布局。  
   - **竖屏手机优先可读**：窄屏下内容不能被「横向挤扁」到几乎不可玩。  
   - 方向性玩法要随形态调整，例如塔防类（阳光菜园等）：**手机/竖屏用上下对战**（敌从上向下，基地在下方）；宽屏横屏可用左右或同样上下，但格子比例要舒服。  
   - 操作热区（按钮、摇杆、选卡）放在拇指易达区；HUD 勿挡住主战场。  
   - 自测至少：桌面宽屏 + 手机竖屏（约 390×844）两种视口。

## 工程习惯

- 自适配画布（`devicePixelRatio` + resize）。
- 主循环 `requestAnimationFrame`，逻辑与绘制分离。
- 接入中心时补 `tools.<id>.title` / `desc`（en + zh-CN）。
- 体积尽量克制；复杂系统可完整，但避免无用抽象层。
- 引入引擎时：**钉死版本号**（如 `three@0.170.0`），写进 HTML 注释；不要用无版本 `latest`。

## 例外：明确要求「网上免费素材」的对战/演出向游戏

若用户明确要求使用免费素材并追求打击感/特效：

- 允许本地打包 **CC0 / 作者声明可自由使用** 的精灵图与音效（放入 `public/assets/game/<name>/`）。
- 必须附 `CREDITS.txt`（来源链接 + 许可）。
- 禁止付费素材、不明授权热链。
- 仍可同时使用引擎；仍需触屏、localStorage、双端布局；音效按用户要求。
- 打击感应包含：hitstop、震屏、受击闪白、连击数、粒子/冲击波等「手感层」，不能只贴图播放。

## 反例

- 必须加载来源不明的 `*.png` / `*.mp3` 才能玩。  
- 只有桌面键鼠、手机完全不能操作。  
- 横屏逻辑硬塞进竖屏导致战场被压成一条缝。  
- 刷新即丢进度且无最高分记录（极简原型除外，合并前应补存档）。  
- 声称「顶级打击感」却无 hitstop / 震屏 / 连招反馈。  
- 3D 游戏无故拒绝引擎、硬用几千行裸 WebGL 重复造轮子（用户未禁止引擎时）。
