---
description: 工具页全局 UI：手机居中、按钮样式、侧栏导航约定
alwaysApply: true
---

# Tool Basecamp 全局 UI 约定

## 样式引入（新页面）

- **工具页**（`.container` + 侧栏 + `.tool-card`）：必须同时引入 `base.css` 与 `docs/docs-common.css`（顺序：base → docs-common → 页面专用 CSS）。
- **门户 / Hub 页**（首页、游戏、内容、音乐等）：至少引入 `base.css`；若使用 `.action-row` / `.error-box` / `.tip-box` / `.tool-card` 等通用组件，**也应引入 `docs-common.css`**，不要只靠页面内联 CSS 补洞。
- **单文件游戏页**可例外（自有 `games-common` 等），但仍须遵守 `.tb-btn` 与并列按钮间距约定。
- 改了 `base.css` / `docs-common.css` 时，给引用处加或递增 `?v=` 缓存参数。

## 按钮（必须统一）

- 所有操作按钮只用 `class="tb-btn"`（需要撑满时加 `w-full`）。
- **唯一样式源**：`public/css/base.css` 的 `.tb-btn`（蓝色 `#2563eb`、圆角胶囊）。
- **禁止**在页面 CSS 里给 `.tb-btn` 改背景色（绿/红/灰等）。布局可用 flex/grid，不要覆盖 `background` / `color`。
- 操作区容器用 `.action-row`（`base.css` / `docs-common.css`）：`display:flex; gap:12px; flex-wrap:wrap`。
- **并列按钮必须有间距**：多个 `.tb-btn` / `a.tb-btn` 并排放时，外层必须包 `.action-row`（或等价 `gap: 12px` 的 flex/grid）。**禁止**两个蓝色按钮紧贴成一块；**禁止**用 margin 为 0 的相邻按钮硬挤。
- 不要为单页再造一套按钮类名来改颜色；需要布局时仍用 `.action-row`。
- **文案：主操作「生成」一律短写**。不要用「生成计划 / 生成菜单 / 生成穿搭」等「生成 + 产物名」；中文键值就写 `生成`，英文 `Generate`。菜谱等需区分产物的例外（如「生成菜谱」）须用户明确要求后再加后缀。

```html
<!-- ✅ -->
<div class="action-row">
  <button type="button" class="tb-btn">计算</button>
  <button type="button" class="tb-btn">清空</button>
</div>

<!-- ❌ 不要：无间距、紧贴 -->
<button class="tb-btn">播放</button><button class="tb-btn">下载</button>

<!-- ❌ 不要 -->
<button class="tb-btn my-green-btn">计算</button>
```

## 手机左右居中（框架层）

- 工具页外层：`.container` > `.sidebar` + `.content` > `.tool-card`。
- 手机端（`max-width: 768px`）规则在 `base.css` / `docs-common.css`：
  - 关闭时侧栏 `display: none`（不占位）；打开时 `fixed` 抽屉。
  - `.content` / `.tool-card`：`width: 100%`，左右 `margin: 0`，禁止 `width:100%` + 左右 margin 同时用。
- **`.tool-card` 桌面样式必须写在对应 `@media` 手机覆盖之前**，避免级联把手机 margin 冲掉。
- 改居中只动框架 CSS，不要在每个工具页单独打补丁。

## 侧栏导航

- 工具页侧栏顶栏「返回首页」用 `.logo-home-btn`，唯一回首页入口。
- 不要再加重复的「全部工具」链接（手机顶栏 / 侧栏底栏曾因重复已去掉）。

## 工具标题（侧栏 vs 内容区，框架层）

- 标准结构仍保留 `.tool-header`（可含 `h1` + 可选说明 `p`）；`base.js` 会注入 `.tool-mobile-bar` 显示当前工具名。
- **手机**（`max-width: 768px`）：标题只在顶栏 `.tool-mobile-bar`；`.tool-mobile-bar ~ .tool-header` 隐藏（`docs-common.css`）。
- **桌面**（`min-width: 769px`、有侧栏时）：侧栏高亮已表示当前工具，隐藏与侧栏重复的 `.tool-header > h1`；若没有说明 `p`，整块 `.tool-header` 一并隐藏（`base.css`）。
- **禁止**在单个工具页用自定义 CSS / 删掉 `h1` 来「去重」；改行为只动 `base.css` / `docs-common.css`。
- 说明文案需要桌面可见时，放在 `.tool-header > p`，不要塞进侧栏。

## 不要默认自动滚动

- **默认不要**在界面变化后自动 `scrollTop = scrollHeight` / `scrollTo` / `scrollIntoView`。
- **禁止**在切换选项、芯片、筛选、行列预设、勾选等操作时滚动页面。
- 仅当用户明确要求，或结果区在首屏完全看不到且确有必要时，才在「生成/上传出结果」后滚到底；清单 CRUD、多图拼图改选项等一律不滚。

## 修改后

- 改了 `base.css` / `docs-common.css` 时，给引用处加或递增 `?v=` 缓存参数。
