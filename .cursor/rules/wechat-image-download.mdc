---
description: 微信/手机内图片下载与保存交互约定（全站统一）
alwaysApply: true
---

# 微信内下载 / 保存图片

微信内置浏览器 **无法可靠触发** `a.download`，也无法用普通 H5 **弹出**「转发给朋友」面板（需微信 JS-SDK + 公众号配置，本站默认不做）。

## 统一交互（结果图 / 可下载图片）

1. **点「下载」**  
   - 微信内：**只提示**，**不要**自动弹预览。  
   - 提示文案要点：长按图片「保存到手机 / 转发给朋友」，或右上角 ··· → **在浏览器中打开**再下载。  
   - 用 `TBImageCloud.downloadBlob(src, name, { tipEl, errorEl })`，保证 `errorEl`/`tipEl` 有可见反馈。

2. **点图片** → 打开大图预览（`TBImageCloud.bindImagePreview(img)` / `openSavePreview`）。  
   - 预览里可长按，使用微信原生「保存到手机 / 转发给朋友」。

3. **展示用 URL**  
   - 微信内优先 **data URL**（`b64ToDataUrl` / `displayImageSrc`），避免 `blob:` 导致长按保存失败。  
   - 非微信可用 object URL。

4. **不要**在下载按钮里自动 `openSavePreview`。

## 适用范围

- **以后新增**带图片下载的工具 / 模块：必须按本规则实现（复用 `image-cloud-client.js`）。  
- **已有模块**：改到下载相关代码时一并迁到上述 API；未改动的旧页保持原样，不要求一次性全站重写。  
- PDF / ZIP 等非图片文件：微信内同样无法强制下载时，至少提示「请在浏览器中打开」；不要套图片长按逻辑。

## 文案 key（中英都要有）

- `tools.imageCloud.wechatBanner` / `wechatSaveTip` / `longPressSave` / `iosSaveTip` / `saveFailed` / `closePreview`
