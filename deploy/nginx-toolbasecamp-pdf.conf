server {
    listen 80;
    listen [::]:80;
    server_name pdf.toolbasecamp.com;

    client_max_body_size 200m;

    location = /locale-bootstrap.js {
        alias /opt/toolbasecamp-deploy/pdf-locale-bootstrap.js;
        default_type application/javascript;
        add_header Cache-Control "public, max-age=3600";
    }

    location = /portal-home-bar.css {
        alias /opt/toolbasecamp-deploy/portal-home-bar.css;
        default_type text/css;
        add_header Cache-Control "public, max-age=3600";
    }

    location = /portal-home-bar.js {
        alias /opt/toolbasecamp-deploy/portal-home-bar.js;
        default_type application/javascript;
        add_header Cache-Control "public, max-age=3600";
    }

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Accept-Encoding "";
        proxy_request_buffering off;
        proxy_read_timeout 1800s;
        proxy_connect_timeout 120s;
        proxy_send_timeout 1800s;
        proxy_buffering off;
        sub_filter '</head>' '<link rel="stylesheet" href="/portal-home-bar.css"><script src="/locale-bootstrap.js"></script><script src="/portal-home-bar.js"></script></head>';
        sub_filter_once on;
        sub_filter_types text/html;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name pdf.toolbasecamp.com;

    ssl_certificate /etc/letsencrypt/live/toolbasecamp.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/toolbasecamp.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 200m;

    location = /locale-bootstrap.js {
        alias /opt/toolbasecamp-deploy/pdf-locale-bootstrap.js;
        default_type application/javascript;
        add_header Cache-Control "public, max-age=3600";
    }

    location = /portal-home-bar.css {
        alias /opt/toolbasecamp-deploy/portal-home-bar.css;
        default_type text/css;
        add_header Cache-Control "public, max-age=3600";
    }

    location = /portal-home-bar.js {
        alias /opt/toolbasecamp-deploy/portal-home-bar.js;
        default_type application/javascript;
        add_header Cache-Control "public, max-age=3600";
    }

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Accept-Encoding "";
        proxy_request_buffering off;
        proxy_read_timeout 1800s;
        proxy_connect_timeout 120s;
        proxy_send_timeout 1800s;
        proxy_buffering off;
        sub_filter '</head>' '<link rel="stylesheet" href="/portal-home-bar.css"><script src="/locale-bootstrap.js"></script><script src="/portal-home-bar.js"></script></head>';
        sub_filter_once on;
        sub_filter_types text/html;
    }
}
