# three.js (vendored)

Pinned **0.170.0** for Home-PC「图生 3D」preview.

Pages use `importmap` → this folder. **Do not** load from `cdn.jsdelivr.net` (slow/blocked refresh in CN).

Upgrade: replace the version directory and update `image-to-3d.html` importmap.
